# -*- mode: snippet; require-final-newline: nil -*-
# name: spock-tc
# key: spock-tc
# mode: groovy-mode
# --
def "${1:test}"() {
 	${2: setup:
	     $3
        }
        ${4: when:
	     $5
        }
	${6: then:
             $7
        }
        $0
}
